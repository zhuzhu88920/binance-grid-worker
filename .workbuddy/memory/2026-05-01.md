# 2026-05-01 工作记录

## 部署到 Cloudflare Workers
- Worker 地址: https://binance-grid-worker.andox.workers.dev
- KV namespace ID: 3ec5780ee6234d97863cba22a28beed3
- Account ID: 7eeac3798da0cafbdd84901d4d9fab18
- GitHub repo: https://github.com/zhuzhu88920/binance-grid-worker
- GitHub Actions 自动部署已配置（需在 repo settings 添加 CLOUDFLARE_API_TOKEN secret）

## 修复记录
1. 推送时间显示 UTC 而非北京时间 → 改用 toLocaleString('zh-CN', { timeZone: 'Asia/Shanghai' })
2. 推送中文乱码 → GET+URLSearchParams 改为 POST+JSON 发 Bark
3. cron 不触发推送 → 根本原因: KV 里的 user_list key 写错了
4. cron 兜底修复 → processAllUsers 在 KV 用户列表为空时，回退读 env.BINANCE_USER_ID

## 架构简化（2026-05-01）
- 凭证改为从环境变量读取（USER1_COOKIE / USER1_CSRF_TOKEN / USER1_BARK_KEY / USER1_ID）
- 多用户支持 USER2_* USER3_* 等前缀，最多 9 个；单用户可用 COOKIE/CSRF_TOKEN/BARK_KEY
- KV 只保留 last_matched_counts 持久化
- 删除 KV 凭证存储和用户列表管理相关逻辑
- 删除冗余 API 接口（/api/add /api/list /api/debug /api/env）
- 保留 /health /api/users /api/trigger /api/test
- 新增 .gitignore，忽略 .dev.vars .wrangler/ node_modules/
- CF 旧 secrets（BINANCE_COOKIE/BINANCE_CSRF_TOKEN/BINANCE_USER_ID）待迁移为 USER1_* 格式

## 用户信息
- userId: zhuzhu（USER1_ID）
- BARK_KEY: YwSqmvunLvMV8a3Nn4gS7c（USER1_BARK_KEY）
