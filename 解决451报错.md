太棒了！经历了一波三折，我们终于把最难的底层逻辑彻底打通了。这个经验非常有价值，因为**“NPM 默认 Header 导致 AWS CloudFront 403 拦截”**是一个巨坑，无数人在做反向代理时都会栽在这里。

下面是为你精心编写的 `README.md` 文档，排版清晰，逻辑严密，非常适合直接发布到 GitHub 分享给其他开发者！

---

# 🚀 解决 Cloudflare Workers 请求 Binance API 触发 HTTP 451 限制的终极方案

## 📖 背景与问题痛点

当我们使用 **Cloudflare Workers (定时任务 Cron Triggers)** 自动抓取 Binance (币安) 接口数据时，经常会遇到以下报错：
```json
HTTP 451: {
  "code": 0,
  "msg": "Service unavailable from a restricted location according to 'b. Eligibility'..."
}
```
**原因分析：** 
Binance 严格封锁了美国 IP。而 Cloudflare 的 Cron Triggers 定时任务会自动分配执行节点（通常是离部署地较近的节点，或者负载均衡分配的节点，如 `ORD` 芝加哥节点）。一旦 Worker 被分配到美国节点执行，请求就会带上美国 IP，从而触发 451 封锁。

**解决方案思路：** 
由于无法指定 Cloudflare 定时任务的执行国家，我们需要引入一台**便宜的亚洲 VPS（如香港节点）**，使用 **Nginx Proxy Manager (NPM)** 搭建一个纯粹的反向代理。让 CF Worker 把请求发给香港 VPS，再由香港 VPS 转发给 Binance。

---

## 🛠️ 准备工作

1. 一台位于亚洲（推荐香港、台湾、日本等 Binance 支持的地区）的 VPS。
2. VPS 上已安装 Docker 及 [Nginx Proxy Manager (NPM)](https://nginxproxymanager.com/)。
3. 一个域名（免费域名亦可，如 `yourdomain.com`）。

---

## ⚙️ 详细操作步骤

### 第一步：域名解析 (DNS)
在你的域名 DNS 控制台（如 Cloudflare），添加两个子域名的 `A 记录`，指向你的亚洲 VPS IP：
* `binance.yourdomain.com` (用于代理 Binance 主站 API)
* `fapi.yourdomain.com` (用于代理 Binance 合约行情 API)

> 💡 **提示**：如果是免费域名，极易被运营商在 HTTP 明文下注入恶意广告 JS 脚本导致 JSON 解析失败。**强烈建议在 Cloudflare 中开启这俩域名的“小黄云 ☁️”（开启代理），并强制使用 HTTPS！**

### 第二步：配置 Nginx Proxy Manager (突破 AWS 拦截的核心！)

⚠️ **高能预警：不要直接在 Proxy Host 的 "Advanced" 标签里写配置！**
NPM 默认会加上 `X-Forwarded-Host` 等代理追踪头，这会直接被 Binance 前端的 **AWS CloudFront 防火墙判定为请求走私**，从而拦截并报 `403 ERROR`。

我们需要使用 **Custom locations** 来彻底覆盖 NPM 的默认规则：

#### 1. 添加 Binance 主站代理 (`www.binance.com`)
1. 登录 NPM，点击 **Add Proxy Host**。
2. **Details** 标签：
   * Domain Names: `binance.yourdomain.com`
   * Scheme: `https` *(必须选 https)*
   * Forward Hostname / IP: `www.binance.com`
   * Forward Port: `443`
3. **Custom locations** 标签（✨ **核心步骤**）：
   * 点击 `Add location`
   * **Define location**: 填入 `/`
   * **Scheme**: `https`
   * **Forward Hostname / IP**: `www.binance.com`
   * **Forward Port**: `443`
   * 点击旁边的 **齿轮图标 (⚙️)**，在弹出的代码框中粘贴以下代码：
     ```nginx
     proxy_set_header Host www.binance.com;
     proxy_ssl_server_name on;
     proxy_ssl_name www.binance.com;
     # 强制清空 NPM 默认添加的所有代理追踪头，防止 AWS CloudFront 报错 403
     proxy_set_header X-Real-IP "";
     proxy_set_header X-Forwarded-For "";
     proxy_set_header X-Forwarded-Host "";
     proxy_set_header X-Forwarded-Proto "";
     proxy_set_header X-Forwarded-Port "";
     ```
4. 点击 **Save** 保存。

#### 2. 添加 Binance 合约代理 (`fapi.binance.com`)
重复上述步骤，仅仅把对应的域名替换掉即可：
* Domain Names: `fapi.yourdomain.com`
* Forward Hostname: `fapi.binance.com`
* Custom locations 的齿轮代码中，将所有的 `www.binance.com` 替换为 `fapi.binance.com`。

---

### 第三步：验证代理是否成功穿透

在任意终端（或你的 VPS 上）执行以下 `curl` 命令，测试代理是否畅通：

```bash
curl -s --max-time 10 -w "\nHTTP: %{http_code}" "http://binance.yourdomain.com/bapi/futures/v2/private/future/grid/query-open-grids" -X POST -H "Content-Type: application/json" -H "User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36" -d '{}'
```

✅ **成功标志**：
如果你看到返回以下内容，说明代理完美穿透了 AWS 防火墙并到达了币安服务器！
```json
{"code":"100001005","message":"Please log in first.","success":false}
HTTP: 401
```
*(注：返回 401 是因为我们没有带上 Cookie，这是正常的，证明网络已经通了)*

❌ **失败标志**：
* 如果返回 `404 Not Found`：NPM 配置错误，或者域名没解析对。
* 如果返回 `403 ERROR (Generated by cloudfront)`：说明你在 NPM 里的 Header 清除代码没生效，请回去检查 Custom locations 配置！

---

### 第四步：修改 Cloudflare Worker 代码（优雅适配）

为了防止自己的代理域名被公开泄露，建议**不要硬编码**在代码里，而是通过**环境变量**传入，并设置官方直连作为默认回退。

在你的 CF Worker 代码中，修改 `fetch` 请求的 Base URL：

```javascript
// 获取主站 API (支持读取环境变量中的自建代理)
async function fetchOpenGrids(headers, env) {
  const baseUrl = env.BAPI_BASE_URL || 'https://www.binance.com';
  const res = await fetch(`${baseUrl}/bapi/futures/v2/private/future/grid/query-open-grids`, {
    method: 'POST',
    headers,
    body: JSON.stringify({}),
  });
  // ...省略后续逻辑
}

// 获取合约行情 API
async function fetchMarkPrice(symbol, env) {
  const fapiUrl = env.FAPI_BASE_URL || 'https://fapi.binance.com';
  const res = await fetch(`${fapiUrl}/fapi/v1/premiumIndex?symbol=${symbol}`);
  // ...省略后续逻辑
}
```

**配置环境变量：**
在 Cloudflare Worker 的 `Settings -> Variables and Secrets` (或本地 `.dev.vars` 文件) 中添加：
```text
BAPI_BASE_URL=https://binance.yourdomain.com
FAPI_BASE_URL=https://fapi.yourdomain.com
```

---

## 🎉 总结
通过以上方案，即使 Cloudflare Worker 的执行节点飘到了美国芝加哥 (ORD)，它也会先将请求发送到你的香港 VPS，再由香港 VPS 完美伪装成原生请求访问 Binance API，**永久告别 HTTP 451 和 CloudFront 403 错误！**

---
*💡 如果这个指南帮到了你，欢迎给本项目点个 Star ⭐！*