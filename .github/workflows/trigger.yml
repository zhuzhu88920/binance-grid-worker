name: Trigger Worker

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: POST /api/trigger
        run: |
          curl -s -X POST "${{ secrets.WORKER_URL }}/api/trigger"
